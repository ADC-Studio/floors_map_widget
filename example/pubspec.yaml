name: example
description: "Example Flutter project to use Floors Map Widget"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0-323.0.dev

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  xml: ^6.5.0
  floors_map_widget: 
    path: '../.'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/example.svg